// const name = ["first", "last"];
// const email = ["@evolveu.ca"];
// var result = [];

// function makeEmail(name) {
//     for (var i = 0; i < name.length; i++) {
//         return (result.concat([name[0] + name[1] + "." + email[0]);]))
//     return (result[i] = name[0] + name[1] +
//         "." + email[0]);
// }
// }

// alert(arr.concat([3, 4]));
// const makeEmail = (name) =


    // makeEmail("bill, "smit");